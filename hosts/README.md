# Hosts configurations

Each directory in here holds configuration that is specific to each host. This
can include a NixOS module, a Home Manager module, hardware configuration, and
disko configuration.

All hosts are named after rocks or minerals. It's just a theme I chose.
